# The Render backend's full URL, no trailing slash. Vercel bakes this in at
# build time, so it must be set as a Vercel Environment Variable (Project
# Settings -> Environment Variables) before you deploy, not just in this file.
VITE_API_URL=https://oyo10x-api.onrender.com
